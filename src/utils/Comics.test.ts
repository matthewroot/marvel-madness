import Comics from './Comics';

describe('Comics', () => {
  test('yearlyAppearances() returns the number of appearances per year', () => {
    // Should look like:
    // 2001: 3
    // 2003: 12
    // 2005: 2
  });
});
